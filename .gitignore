# Personal deployment notes & scripts that aren't part of the
# public getting-started flow (klimax, kc CLI, GCP-specific
# bootstrapping). Kept for the author's local reference.
docs/private/

# IDE config
.idea/

# Local OIDC secret produced by scripts/keycloak-bootstrap.sh.
# Don't commit the real client secret.
deploy/envoy-gateway/secret.yaml

# Go / build artefacts (anchored — must not match helm-chart/porthole/)
/porthole
*.test
*.out
